# n8n MCP Server Configuration
# Copy this file to .env and fill in your values
# IMPORTANT: Never commit .env to version control!

# ==========================================
# Primary Configuration (leonardsellem/n8n-mcp-server)
# ==========================================
# The REST API URL for n8n (includes /api/v1 path)
N8N_API_URL=http://localhost:5678/api/v1

# n8n API Key
# Generate this in n8n: Settings → API → Create API Key
# IMPORTANT: Keep this secret! Never commit or share this key.
N8N_API_KEY=

# ==========================================
# Alternative Configuration (illuminaresolutions)
# ==========================================
# The base URL of your n8n instance (no /api/v1 path)
N8N_BASE_URL=http://localhost:5678

# ==========================================
# Optional: Custom n8n Configuration
# ==========================================
# Uncomment and configure as needed

# N8N_PORT=5678
# N8N_PROTOCOL=http
# N8N_HOST=localhost

# ==========================================
# Production/Remote n8n Instances
# ==========================================
# For production HTTPS instances:
# N8N_API_URL=https://n8n.yourdomain.com/api/v1
# N8N_BASE_URL=https://n8n.yourdomain.com
# N8N_API_KEY=your-production-api-key

# ==========================================
# MCP Server Selection
# ==========================================
# All four MCP servers are configured:
# 1. n8n-production (leonardsellem) - Primary for workflow management
# 2. n8n-workflow-builder (czlonkowski) - AI-assisted workflow design
# 3. n8n-workflow-creator (ifmelate) - Programmatic workflow creation
# 4. n8n-illuminare (illuminaresolutions) - Alternative server
